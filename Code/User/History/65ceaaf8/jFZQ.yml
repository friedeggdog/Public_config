version: "3.9"

services:
  server:
    container_name: festember_tshirt_frontend
    image: festember_tshirt_frontend_image
    build:
      context: .
    ports:
      - "80:80"
    restart: always
